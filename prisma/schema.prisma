// prisma/schema.prisma
//
// Generator and datasource configuration only.
// Models are defined in prisma/models/*.prisma

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}
